import { Entity, ServiceMixin, TransformMixin } from '../../types';
export declare function serviceMixin(entity: Entity, transform: TransformMixin, connectionName: string, tenantId: string): ServiceMixin;
